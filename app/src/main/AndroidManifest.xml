<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="in.ac.nitc.hallallocator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Faculty_Main"></activity>
        <activity
            android:name=".Faculty_dash"
            android:label="@string/title_activity_faculty_dash"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Student_Main" />
        <activity android:name=".Incharge_Main" />
        <activity android:name=".faculty_form" />
        <activity android:name=".student_form"></activity>
    </application>

</manifest>